import Comment from '../Comment';

export default function CommentExample() {
  const sampleComments = [];

  return (
    <div className="p-8 space-y-6 max-w-3xl">
      <p className="text-gray-500">No sample comments available.</p>
    </div>
  );
}
